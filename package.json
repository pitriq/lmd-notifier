{
  "name": "lmd-notifier",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "export $(awk '/environment:/,/^[^ ]/ {if ($0 ~ /^[ ]*-/) {print $0}}' compose.override.yml | sed 's/- //g' | sed 's/^[[:space:]]*//g' | sed 's/: /=/g' | xargs) && bun --watch run index.ts"
  },
  "trustedDependencies": ["puppeteer"],
  "type": "module",
  "dependencies": {
    "puppeteer": "^24.8.1"
  }
}
